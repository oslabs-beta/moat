apiVersion: v1
kind: Service
metadata:
  name: codeforge-service
  namespace: codeforge-app
  labels:
    app: codeforge-app
spec:
  selector:
    app: codeforge-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
